{
    "name": "zagware",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "runArgs": [
        "--cap-add=SYS_PTRACE",
        "--security-opt",
        "seccomp=unconfined"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "mhutchie.git-graph",
                "rust-lang.rust-analyzer",
                "ReprEng.csv"
            ]
        }
    }
}